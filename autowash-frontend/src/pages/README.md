# Frontend Pages

Thu muc nay chua HTML theo tung workspace:

- `auth`: login, register, verify OTP
- `customer`: trang chu khach hang va dat lich
- `staff`: hang doi van hanh
- `admin`: dashboard quan tri

Moi page chi khai bao markup va link den `../styles/main.css` cung `../scripts/main.js`. Logic hanh vi nam trong `src/scripts/modules`.
